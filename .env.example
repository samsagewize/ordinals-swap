# Copy this to .env.local and fill in values

# Optional: Your own Hiro API key for higher rate limits
# Get one free at https://platform.hiro.so
REACT_APP_HIRO_API_KEY=

# Optional: Set to 'testnet' to use Bitcoin testnet instead of mainnet
REACT_APP_NETWORK=mainnet
